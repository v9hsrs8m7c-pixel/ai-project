# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local build / temp artifacts (keep repo clean)
.next-b2/
.next-verify/
*.log
test.js

# opennext / cloudflare build output
.open-next/
.workers/
.wrangler/

# let Cloudflare Pages run `npm install` (its default `npm ci` requires a synced lockfile)
package-lock.json

# GM feed import cache (regenerated by scripts/import-gm-games.mjs)
scripts/.cache/

# local deploy credentials (contains Cloudflare API token — never commit)
CF_DEPLOY_SECRETS.md
