# Cloudflare Pages 缓存规则
# 规则从上到下匹配，越具体的规则优先。

# Next.js 打包的静态资源文件名带 hash，内容永不变，可缓存 1 年
/_next/static/**
  Cache-Control: public, max-age=31536000, immutable

# HTML 页面：浏览器缓存 1 小时，Cloudflare 边缘缓存 24 小时
# 这样 HTML 也会被 CDN 命中（cf-cache-status -> HIT）。
# 更新内容后重新部署会自动清缓存，访客立即看到新版。
/*
  Cache-Control: public, max-age=3600, s-maxage=86400
